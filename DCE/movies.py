def slide_window(
		out_filename,
		tau=10,
		ds_rate=1,
		m=2,
		save_movie=True,
		framerate=1
):
	# implement with using signals module
	# write new waveform / window / crop plotting stuff in signals
	pass